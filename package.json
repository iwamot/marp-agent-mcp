{
  "name": "marp-agent-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Marp slide generation MCP server with preview UI",
  "license": "MIT",
  "main": "dist/server.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --noEmit && cross-env INPUT=mcp-app.html vite build && tsc -p tsconfig.server.json && bun build server.ts --outdir dist --target node && bun build main.ts --outfile dist/index.js --target node --external \"./server.js\" --banner \"#!/usr/bin/env node\"",
    "watch": "cross-env INPUT=mcp-app.html vite build --watch",
    "serve": "bun --watch main.ts",
    "serve:stdio": "bun main.ts --stdio",
    "start": "cross-env NODE_ENV=development aube run build && aube run serve",
    "start:stdio": "cross-env NODE_ENV=development aube run build 1>&2 && aube run serve:stdio",
    "dev": "cross-env NODE_ENV=development concurrently \"aube run watch\" \"aube run serve\"",
    "check": "biome check",
    "check:write": "biome check --write",
    "prune": "pnpm-override-prune --fix",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.server.json --noEmit",
    "test": "bun test --coverage --coverage-reporter=lcov --coverage-reporter=text"
  },
  "dependencies": {
    "@marp-team/marp-core": "4.3.1",
    "@modelcontextprotocol/ext-apps": "1.7.4",
    "@modelcontextprotocol/sdk": "1.29.0",
    "@types/cors": "2.8.19",
    "@types/express": "5.0.6",
    "@types/node": "24.13.3",
    "concurrently": "10.0.3",
    "cors": "2.8.6",
    "cross-env": "10.1.0",
    "express": "5.2.1",
    "get-east-asian-width": "1.6.0",
    "typescript": "7.0.2",
    "vite": "8.1.4",
    "vite-plugin-singlefile": "2.3.3",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "1.3.14"
  },
  "types": "dist/server.d.ts",
  "exports": {
    ".": {
      "types": "./dist/server.d.ts",
      "default": "./dist/server.js"
    }
  },
  "bin": {
    "marp-agent-mcp": "dist/index.js"
  }
}
